// Placeholder for future Firebase Firestore integration
// Currently using localStorage via domain services

export const firestoreClient = {
  // Future: Firebase SDK initialization and helpers
  // Example:
  // collection: (name: string) => firestore.collection(name),
  // doc: (path: string) => firestore.doc(path),
  // etc.
};
