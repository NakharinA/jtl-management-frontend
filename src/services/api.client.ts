// Placeholder for future NestJS API integration
// Currently using localStorage via domain services

export const apiClient = {
  // Future: axios instance with base URL and auth headers
  // Example:
  // get: (url: string) => axios.get(url),
  // post: (url: string, data: any) => axios.post(url, data),
  // etc.
};
